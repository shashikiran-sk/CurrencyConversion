version: "3.3"
services:
    currency-conversion:
        container_name: currency-conversion
        build:
            context: .
            dockerfile: Dockerfile
        image: currencyconversion:v1.0.0
        ports:
            - 4201:4200
            - 8080:80
